<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberSQL">

    <resultMap id="MemberResponseDTO" type="com.example.project.dto.MemberResponseDTO">
        <result property = "Id" column="m_Id"/>
        <result property = "name" column="m_name"/>
        <result property = "username" column="m_username"/>
        <result property="password" column="m_password"/>
        <result property = "role" column="m_role"/>
    </resultMap>

    <select id="searchMember" parameterType="String" resultMap="MemberResponseDTO">
        SELECT
            m_Id,
            m_name,
            m_username,
            m_password,
            m_role
        FROM member
        WHERE m_username = #{username}
    </select>

    <insert id="save" parameterType="MemberRequestDTO" useGeneratedKeys="true" keyProperty="insertId">
        INSERT INTO member (
                            m_name,
                            m_username,
                            m_password,
                            m_role
        ) VALUES (
                  #{name},
                  #{username},
                  #{password},
                  #{role}
                         )
    </insert>

    <select id="passwordCheck" parameterType="LoginRequestDTO" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE m_username = #{username}
        AND m_password = #{password}
    </select>
</mapper>